# Demo App

Minimal Next.js demo for rapid local testing and screenshots.

## Run

```bash
cd demo
npm install
npm run dev
```

Open `http://localhost:3200`.

Use `Cmd/Ctrl + Shift + U` to open the feedback overlay.
